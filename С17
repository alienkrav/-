user_string = input().split(" ")
for i in range(len(user_string)-1):
    for j in range(i+1, len(user_string)):
        if user_string[i] > user_string[j]:
            user_string[i],user_string[j] = user_string[j],user_string[i]

print(" ".join(user_string))